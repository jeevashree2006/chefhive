{
  "name": "chefhive",
  "version": "2.0.0",
  "private": true,
  "description": "ChefHive: book traditional regional cooks for occasions. React client + Express API + MySQL.",
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix client install",
    "db:setup": "npm --prefix server run db:setup",
    "seed": "npm --prefix server run seed",
    "dev:api": "npm --prefix server run dev",
    "dev:web": "npm --prefix client run dev",
    "dev": "concurrently -n api,web -c yellow,cyan \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "build": "npm --prefix client run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
